# GENERATED FROM Dockerfile.template-curl
FROM vicamo/ubuntu-debootstrap:wily-powerpc

RUN apt-get update \
	&& apt-get install -y --no-install-recommends \
		ca-certificates \
		curl \
		wget \
	&& apt-get clean \
	&& rm -rf /var/lib/apt/lists/*_dists_*
